package com.example.smartfit.viewmodel

import androidx.lifecycle.ViewModel
import com.example.smartfit.data.repository.ActivityRepository

class ProfileViewModel(private val repo: ActivityRepository) : ViewModel() {
    // placeholder for methods to read/write DataStore via repository or AppContainer
}
